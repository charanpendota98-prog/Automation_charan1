# nginx alternative (Caddy vadakapote). Certbot: sudo certbot --nginx -d exams.college.edu
server {
	listen 80;
	listen [::]:80;
	server_name exams.college.edu;
	return 301 https://$host$request_uri;
}

server {
	listen 443 ssl http2;
	listen [::]:443 ssl http2;
	server_name exams.college.edu;

	ssl_certificate     /etc/letsencrypt/live/exams.college.edu/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/exams.college.edu/privkey.pem;
	ssl_protocols TLSv1.2 TLSv1.3;
	add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
	add_header X-Content-Type-Options nosniff always;
	add_header Referrer-Policy no-referrer always;

	client_max_body_size 12m;   # question bulk upload (CSV/JSON) ki

	location / {
		proxy_pass http://127.0.0.1:8080;
		proxy_http_version 1.1;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_read_timeout 300s;
	}
	access_log /var/log/nginx/exam-portal.log;
}
